# 🧭 AI-Capital Production Hardening Plan
**Goal:** elevate the app from MVP → production-grade with paid-tier readiness  
**Duration:** ~1 week sprint  
**Owner:** Core Dev Team (Backend, Frontend, DevOps)

---

## 1️⃣ SECURITY & MIDDLEWARE (`backend/src/app.ts`)
- [x] Install `helmet`, `express-rate-limit`, `cors`, `cookie-parser` ✅
- [x] Add `helmet()` for HTTP header protection ✅
- [x] Apply `rateLimit()` → 300 req/minute ✅
- [x] Restrict CORS origins to vercel frontend URL + admin dashboard domain ✅
- [x] Enable `credentials: true` ✅
- [ ] Implement CSRF protection (double submit or `csurf`) - TODO
- [x] Set cookies: `httpOnly`, `sameSite=Lax`, `secure` in production ✅
- [x] Add centralized error handler middleware (logs + JSON output) ✅

---

## 2️⃣ REQUEST VALIDATION (`backend/src/middleware/validate.ts`)
- [x] Install `zod` ✅
- [x] Create `validate(schema)` middleware ✅
- [x] Define request schemas in `backend/src/schemas/*` ✅
- [x] Apply validation to all API routes (auth, onboarding, portfolio) ✅
- [x] Reject invalid symbols / missing params before controller logic ✅

---

## 3️⃣ DATA & INDEX OPTIMIZATION (`backend/src/models`)
- [x] Add MongoDB indexes: ✅
      - users: `{ email: 1 } unique` ✅
      - portfolios: `{ userId: 1, type: 1 }` ✅
      - historicaldata: `{ symbol: 1, date: -1 }` ✅
      - watchlist: `{ userId: 1, ticker: 1 }` ✅
- [ ] Add pre-save hook to enforce stock limit per plan - TODO
- [x] Run `Model.ensureIndexes()` on startup ✅
- [ ] Benchmark heavy queries with `.explain()` - TODO

---

## 4️⃣ MARKET-DATA SERVICE (`backend/src/services/stockDataService.ts`)
- [x] Install `lru-cache` ✅
- [x] Implement symbol-scoped LRU cache (max 1000, ttl 10min) ✅
- [x] Add retry/backoff logic with 12 API keys and aggressive retries ✅
- [x] Build provider fallback chain: [AlphaVantage x4, Finnhub x4, FMP x4] ✅
- [x] Add smart key rotation and blacklisting ✅
- [x] Cache historical data with 10-minute TTL ✅
- [x] Log provider latency and cache hit rate ✅

---

## 5️⃣ CRON & JOB STABILITY (`backend/src/services/schedulerService.ts`)
- [ ] Install and connect `redis` (`REDIS_URL`)
- [ ] Implement distributed lock (SET NX PX)
- [ ] Wrap cron jobs with `withLock()` helper
- [ ] Upsert by `(portfolioId, symbol, date)` to prevent duplicates
- [ ] Add job logs: "Skipped run – lock held"
- [ ] Gracefully handle provider downtime (retry next cycle)

---

## 6️⃣ HEALTH & LOGGING
- [x] Install `pino` + `pino-http` ✅
- [x] Add `requestId` middleware (uuid) ✅
- [x] Log method, path, duration, and requestId ✅
- [x] Implement `/healthz` endpoint returning `{ status, uptime }` ✅
- [x] Configure Render health check to `/healthz` ✅
- [x] Integrate Sentry transport ✅

---

## 7️⃣ FRONTEND API CONSOLIDATION (`frontend/lib/api.ts`)
- [ ] Create centralized `api.ts` for all fetch requests
- [ ] Add Zod validation for responses
- [ ] Unify error handling (map status → toast)
- [ ] Use SWR/React-Query for caching
- [ ] Apply consistent base URL from env
- [ ] Add type-safe DTOs for each API call

---

## 8️⃣ FRONTEND UX POLISH
- [ ] Add tooltips for "This month %" vs "Last month %"
- [ ] Display "Last updated: …" under market indices
- [ ] Add plan-based feature flags (Free / Premium / Premium+)
- [ ] Use unified number formatters (currency, percent)
- [ ] Add skeleton loaders for async sections
- [ ] Confirm full mobile responsiveness on major screens

---

## 9️⃣ TESTING INFRASTRUCTURE
- [ ] Install `jest`, `supertest`, `ts-jest`
- [ ] Unit tests:
      - [ ] DecisionEngine scoring
      - [ ] StockDataService fallback chain
      - [ ] Portfolio limit enforcement
- [ ] Integration tests:
      - [ ] Auth → onboarding → portfolio add → decision fetch
- [ ] E2E tests with Playwright:
      - [ ] login → add stock → see decision
- [ ] Add `"test": "jest"` to package.json
- [ ] Configure CI to run tests before deploy

---

## 🔟 DEVOPS & ENVIRONMENT
- [ ] Convert Dockerfile to multi-stage build (build → run)
- [ ] Ensure `NODE_ENV=production` and `npm ci --omit=dev`
- [ ] Add `.env.example` with all required vars (no secrets)
- [ ] Update `docker-compose.yml` to reference `.env`
- [ ] Add Render health checks and env var docs
- [ ] Enable auto-deploy triggers from GitHub main branch
- [ ] Stream Pino logs to Sentry or Logtail

---

## 1️⃣1️⃣ DOCUMENTATION (`/docs`)
- [ ] `Architecture.md` – overview + diagram
- [ ] `DataProviders.md` – API sources + fallback logic
- [ ] `DecisionEngine.md` – weights, signals, risk scoring
- [ ] `Runbook.md` – deploy & troubleshooting
- [ ] Update root `README.md` with production setup + paid tiers

---

## 1️⃣2️⃣ DEPLOY VALIDATION CHECKLIST
- [ ] All env vars configured on Render + Vercel
- [ ] Redis lock tested successfully
- [ ] `/healthz` returns ok in Render dashboard
- [ ] Rate limiter verified (no 429s under load)
- [ ] Sentry receiving backend error events
- [ ] Test Premium user limit enforcement (3 portfolios × 15 stocks)
- [ ] Run end-to-end smoke test on live deployment

---

✅ **After this checklist:**
- Security hardened  
- Jobs idempotent  
- API validated & cached  
- UX reliable  
- Tests automated  
- Ops observable  
→ App officially *production-ready for paid users.*
